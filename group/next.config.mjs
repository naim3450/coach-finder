/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', "techinnovators.com", "lh3.googleusercontent.com"], // Add the domain here
  },
};

export default nextConfig;
